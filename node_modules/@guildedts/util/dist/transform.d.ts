/**
 * Resolve a color from a string, number, RGB array or color preset.
 * Inspired by discord.js'
 * {@link https://github.com/discordjs/discord.js/blob/main/packages/discord.js/src/util/Util.js#L476 color resolver}.
 * @param color The color to resolve.
 * @returns The resolved color.
 * @example
 * // Resolve the color with a hex color.
 * resolveColor(0xFFFFFF);
 * resolveColor('#FFFFFF');
 * // Resolve the color with a color preset.
 * resolveColor('White');
 * // Resolve the color with RGB values.
 * resolveColor([255, 255, 255]);
 */
export declare function resolveColor(color: ColorResolvable): number;
/** The color resolvable type. */
export declare type ColorResolvable = `#${string}` | keyof typeof Color | 'Random' | number | [red: number, green: number, blue: number];
/** A set of color presets. */
export declare enum Color {
    Default = 0,
    White = 16777215,
    Aqua = 1752220,
    Green = 5763719,
    Blue = 3447003,
    Yellow = 16705372,
    Purple = 10181046,
    LuminousVividPink = 15277667,
    Fuchsia = 15418782,
    Gold = 15844367,
    Gilded = 16106496,
    Orange = 15105570,
    Red = 15548997,
    Grey = 9807270,
    Navy = 3426654,
    DarkAqua = 1146986,
    DarkGreen = 2067276,
    DarkBlue = 2123412,
    DarkPurple = 7419530,
    DarkVividPink = 11342935,
    DarkGold = 12745742,
    DarkOrange = 11027200,
    DarkRed = 10038562,
    DarkGrey = 9936031,
    DarkerGrey = 8359053,
    LightGrey = 12370112,
    DarkNavy = 2899536,
    Blurple = 5793266,
    Greyple = 10070709,
    DarkButNotBlack = 2895667,
    NotQuiteBlack = 2303786
}
/**
 * Covert object keys to snake_case.
 * @param target The target object.
 * @returns The snake_case result.
 * @example toSnakeCase({ helloWorld: 'Hello World!' }); // { hello_world: 'Hello World!' }
 */
export declare function toSnakeCase(target: unknown): any;
/**
 * Covert object keys to camelCase.
 * @param target The target object.
 * @returns The camelCase result.
 * @example toCamelCase({ hello_world: 'Hello World!' }); // { helloWorld: 'Hello World!' }
 */
export declare function toCamelCase(target: unknown): any;
//# sourceMappingURL=transform.d.ts.map