import { APIServer } from 'guilded-api-typings';
import { BaseRouter } from '../BaseRouter';
/**
 * The server router for the Guilded REST API.
 * @example new ServerRouter(rest);
 */
export declare class ServerRouter extends BaseRouter {
    /**
     * Fetch a server from Guilded.
     * @param serverId The ID of the server to fetch.
     * @returns The fetched server.
     * @example servers.fetch('abc');
     */
    fetch(serverId: string): Promise<APIServer>;
}
//# sourceMappingURL=ServerRouter.d.ts.map