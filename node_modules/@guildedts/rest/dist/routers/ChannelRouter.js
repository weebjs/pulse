"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ChannelRouter = void 0;
const guilded_api_typings_1 = require("guilded-api-typings");
const BaseRouter_1 = require("./BaseRouter");
/**
 * The channel router for the Guilded REST API.
 * @example new ChannelRouter(rest);
 */
class ChannelRouter extends BaseRouter_1.BaseRouter {
    /**
     * Fetch a channel from Guilded.
     * @param channelId The ID of the channel to fetch.
     * @returns The fetched channel.
     * @example channels.fetch('abc');
     */
    async fetch(channelId) {
        const { channel } = await this.rest.get(guilded_api_typings_1.Routes.channel(channelId));
        return channel;
    }
    /**
     * Create a channel on Guilded.
     * @param payload The payload of the channel.
     * @returns The created channel.
     * @example channels.create({ name: 'Chat', type: 'chat' });
     */
    async create(payload) {
        const { channel } = await this.rest.post(guilded_api_typings_1.Routes.channels, payload);
        return channel;
    }
    /**
     * Edit a channel on Guilded.
     * @param channelId The ID of the channel to edit.
     * @param payload The payload of the channel.
     * @returns The edited channel.
     * @example channels.edit('abc', { name: 'Chat' });
     */
    async edit(channelId, payload) {
        const { channel } = await this.rest.patch(guilded_api_typings_1.Routes.channel(channelId), payload);
        return channel;
    }
    /**
     * Delete a channel from Guilded.
     * @param channelId The ID of the channel to delete.
     * @example channels.delete('abc');
     */
    delete(channelId) {
        return this.rest.delete(guilded_api_typings_1.Routes.channel(channelId));
    }
}
exports.ChannelRouter = ChannelRouter;
//# sourceMappingURL=ChannelRouter.js.map