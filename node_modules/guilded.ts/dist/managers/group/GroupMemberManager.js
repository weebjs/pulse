"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GroupMemberManager = void 0;
const BaseManager_1 = require("../BaseManager");
const ServerMember_1 = require("../../structures/server/ServerMember");
/**
 * The manager of members that belong to a group.
 * @example new GroupMemberManager(group);
 */
class GroupMemberManager extends BaseManager_1.BaseManager {
    group;
    /** @param group The group the members belong to. */
    constructor(group) {
        super(group.client);
        this.group = group;
    }
    /**
     * Add a member to the group.
     * @param member The member to add.
     * @example members.add(member);
     */
    add(member) {
        member = member instanceof ServerMember_1.ServerMember ? member.id : member;
        return this.client.api.groups.addMember(this.group.id, member);
    }
    /**
     * Remove a member from the group.
     * @param member The member to remove.
     * @example members.remove(member);
     */
    remove(member) {
        member = member instanceof ServerMember_1.ServerMember ? member.id : member;
        return this.client.api.groups.removeMember(this.group.id, member);
    }
}
exports.GroupMemberManager = GroupMemberManager;
//# sourceMappingURL=GroupMemberManager.js.map