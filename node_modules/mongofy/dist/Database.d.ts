import { MongoClient } from "mongodb";
import { MongoCollection } from "./index";
export interface DatabaseOptions {
    uri: string;
    name?: string;
    separator?: string;
}
export declare class Database {
    uri: string;
    name: string;
    separator: string;
    client: MongoClient;
    constructor(options: DatabaseOptions);
    collection(name: string): MongoCollection;
}
