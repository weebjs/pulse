import { Collection } from "mongodb";
import { Document } from "./index";
export declare class MongoCollection {
    private collection;
    private separator;
    constructor(collection: Collection, separator: string);
    find(filter: object): Document;
}
