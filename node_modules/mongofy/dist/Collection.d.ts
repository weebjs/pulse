import { Collection, FindOptions } from "mongodb";
import { Database, Data } from "./index";
export declare class MongoCollection {
    database: Database;
    collection: Collection;
    constructor(database: Database, collection: Collection);
    findOne(filter: object, options?: FindOptions): Data;
}
