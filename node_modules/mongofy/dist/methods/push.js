"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.push = void 0;
async function push(self, key, item) {
    if (!key)
        throw new Error("[mongofy] Enter the name of the key");
    if (!item && item !== 0)
        throw new Error("[mongofy] Enter the name of the value");
    let array = [];
    if (await self.has(key)) {
        if (typeof (await self.get(key)) !== "object") {
            array = [];
        }
        else {
            array = await self.get(key);
        }
    }
    array.push(item);
    await self.set(key, array);
    return self.get(key);
}
exports.push = push;
//# sourceMappingURL=push.js.map