"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.add = void 0;
async function add(self, key, number) {
    if (isNaN(number))
        throw new TypeError("[mongofy] Enter a number");
    if (!key)
        throw new Error("[mongofy] Enter the name of the key");
    if (!number)
        throw new Error("[mongofy] Enter the name of the value");
    await self.set(key, Number((await self.get(key))
        ? isNaN(await self.get(key))
            ? Number(number)
            : (await self.get(key)) + Number(number)
        : Number(number)));
    return self.get(key);
}
exports.add = add;
//# sourceMappingURL=add.js.map